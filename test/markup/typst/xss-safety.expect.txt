<span class="hljs-comment">// XSS safety regression: user-supplied source must come back</span>
<span class="hljs-comment">// as escaped text inside spans, not as raw markup that could</span>
<span class="hljs-comment">// break out of the highlight wrapper and execute.</span>

#<span class="hljs-keyword">let</span> user<span class="hljs-emphasis">_input = &quot;&lt;script&gt;alert(1)&lt;/script&gt;&quot;
#let img_</span><span class="hljs-keyword">in</span><span class="hljs-emphasis">_text = &quot;Hello &lt;img src=x onerror=alert(1)&gt; world&quot;

The user wrote: *&lt;script&gt;alert(1)&lt;/script&gt;* in markup.

Number: &lt;script&gt;not a script&lt;/script&gt;
</span>
