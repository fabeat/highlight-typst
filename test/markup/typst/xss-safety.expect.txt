<span class="hljs-comment">// XSS safety regression: user-supplied source must come back</span>
<span class="hljs-comment">// as escaped text inside spans, not as raw markup that could</span>
<span class="hljs-comment">// break out of the highlight wrapper and execute.</span>

<span class="hljs-keyword">#let</span> user_input = <span class="hljs-string">&quot;&lt;script&gt;alert(1)&lt;/script&gt;&quot;</span>
<span class="hljs-keyword">#let</span> img_in_text = <span class="hljs-string">&quot;Hello &lt;img src=x onerror=alert(1)&gt; world&quot;</span>

The user wrote: <span class="hljs-emphasis">*&lt;script&gt;alert(1)&lt;/script&gt;*</span> in markup.

Number: <span class="hljs-symbol">&lt;script&gt;</span>not a script&lt;/script&gt;

