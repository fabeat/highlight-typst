<span class="hljs-comment">// Starter document</span>
<span class="hljs-title">= Hello, Typst!</span>

This is rendered by <span class="hljs-emphasis">*ext-typst*</span> via the plugin.

#<span class="hljs-keyword">let</span> name = <span class="hljs-string">&quot;World&quot;</span>
#<span class="hljs-keyword">let</span> greeting = [Hello, <span class="hljs-function">#name</span>!]

<span class="hljs-function">#greeting</span>

<span class="hljs-title">== Section</span>

<span class="hljs-bullet">- </span>bullet one
<span class="hljs-bullet">- </span>bullet two
  - nested bullet
<span class="hljs-bullet">/ </span>numbered one
<span class="hljs-bullet">+ </span>term: definition

`Inline code` <span class="hljs-keyword">and</span> <span class="hljs-emphasis">*strong*</span> <span class="hljs-keyword">and</span> <span class="hljs-emphasis">_emphasis_</span> <span class="hljs-keyword">and</span> <span class="hljs-emphasis">~subscript~</span>.

#<span class="hljs-keyword">let</span> nums = (<span class="hljs-number">1</span>, <span class="hljs-number">2</span>, <span class="hljs-number">3</span>)
#<span class="hljs-keyword">let</span> total = nums.fold(<span class="hljs-number">0</span>, (<span class="hljs-variable">acc</span>, n) =&gt; acc + n)

Total: <span class="hljs-function">#total</span>.

#<span class="hljs-keyword">set</span> <span class="hljs-built_in">text</span>(<span class="hljs-variable">size</span>: <span class="hljs-number">12pt</span>)
#<span class="hljs-built_in">align</span>(<span class="hljs-variable">center</span>)[Centered <span class="hljs-built_in">text</span>]

